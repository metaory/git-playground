```text
1c
```

```1c
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
4d
```

```4d
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sap-abap
```

```sap-abap
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
abap
```

```abap
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
abc
```

```abc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
abcnotation
```

```abcnotation
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
abnf
```

```abnf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
accesslog
```

```accesslog
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
accesslogs
```

```accesslogs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
actionscript
```

```actionscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
as
```

```as
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ada
```

```ada
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
aiken
```

```aiken
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ak
```

```ak
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ln
```

```ln
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
alan
```

```alan
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
i
```

```i
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
alanif
```

```alanif
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
angelscript
```

```angelscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
asc
```

```asc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
apache
```

```apache
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
apacheconf
```

```apacheconf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
apex
```

```apex
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
applescript
```

```applescript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
osascript
```

```osascript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
arcade
```

```arcade
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
arduino
```

```arduino
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ino
```

```ino
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
arduinocppwarduinolibs
```

```arduinocppwarduinolibs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
armasm
```

```armasm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
arm
```

```arm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
armassembler
```

```armassembler
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
asciidoc
```

```asciidoc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
adoc
```

```adoc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
aspectj
```

```aspectj
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
autohotkey
```

```autohotkey
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
autoit
```

```autoit
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
avrasm
```

```avrasm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
avrassembler
```

```avrassembler
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
awk
```

```awk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mawk
```

```mawk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nawk
```

```nawk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gawk
```

```gawk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ballerina
```

```ballerina
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bal
```

```bal
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bash
```

```bash
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sh
```

```sh
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
zsh
```

```zsh
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
basic
```

```basic
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bbcode
```

```bbcode
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bicep
```

```bicep
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
blade
```

```blade
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bladelaravel
```

```bladelaravel
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bnf
```

```bnf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bqn
```

```bqn
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
brainfuck
```

```brainfuck
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bf
```

```bf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
c
```

```c
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
h
```

```h
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
csharp
```

```csharp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cs
```

```cs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cpp
```

```cpp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hpp
```

```hpp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cc
```

```cc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hh
```

```hh
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
c++
```

```c++
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
h++
```

```h++
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cxx
```

```cxx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hxx
```

```hxx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cal
```

```cal
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
c3
```

```c3
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cos
```

```cos
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cls
```

```cls
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cacheobjectscript
```

```cacheobjectscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
candid
```

```candid
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
did
```

```did
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
capnproto
```

```capnproto
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
capnp
```

```capnp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
chaos
```

```chaos
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
kaos
```

```kaos
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
chapel
```

```chapel
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
chpl
```

```chpl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cisco
```

```cisco
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ciscocli
```

```ciscocli
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
clojure
```

```clojure
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
clj
```

```clj
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cmake
```

```cmake
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cmake.in
```

```cmake.in
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cobol
```

```cobol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
standard-cobol
```

```standard-cobol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
codeowners
```

```codeowners
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
coffeescript
```

```coffeescript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
coffee
```

```coffee
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cson
```

```cson
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
iced
```

```iced
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
coq
```

```coq
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cpc
```

```cpc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cpcdosc
```

```cpcdosc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
crmsh
```

```crmsh
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
crm
```

```crm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pcmk
```

```pcmk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
crystal
```

```crystal
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cr
```

```cr
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
csp
```

```csp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
css
```

```css
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
curl
```

```curl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cypher
```

```cypher
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cypherneo4j
```

```cypherneo4j
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
d
```

```d
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dafny
```

```dafny
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dart
```

```dart
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dpr
```

```dpr
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dfm
```

```dfm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pas
```

```pas
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pascal
```

```pascal
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
delphi
```

```delphi
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
diff
```

```diff
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
patch
```

```patch
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
django
```

```django
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jinja
```

```jinja
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dns
```

```dns
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
zone
```

```zone
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bind
```

```bind
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dnszonefile
```

```dnszonefile
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dockerfile
```

```dockerfile
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
docker
```

```docker
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dos
```

```dos
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
bat
```

```bat
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cmd
```

```cmd
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dsconfig
```

```dsconfig
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dts
```

```dts
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dtsdevicetree
```

```dtsdevicetree
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dust
```

```dust
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dst
```

```dst
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
dylan
```

```dylan
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ebnf
```

```ebnf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
elixir
```

```elixir
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
elm
```

```elm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
erlang
```

```erlang
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
erl
```

```erl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
excel
```

```excel
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xls
```

```xls
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xlsx
```

```xlsx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
extempore
```

```extempore
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xtlang
```

```xtlang
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xtm
```

```xtm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fsharp
```

```fsharp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fs
```

```fs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fsx
```

```fsx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fsi
```

```fsi
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fsscript
```

```fsscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fix
```

```fix
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
flix
```

```flix
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
fortran
```

```fortran
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
f90
```

```f90
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
f95
```

```f95
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
func
```

```func
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gcode
```

```gcode
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nc
```

```nc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gams
```

```gams
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gms
```

```gms
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gauss
```

```gauss
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gss
```

```gss
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
godot
```

```godot
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gdscript
```

```gdscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gherkin
```

```gherkin
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gleam
```

```gleam
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hbs
```

```hbs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
glimmer
```

```glimmer
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
html.hbs
```

```html.hbs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
html.handlebars
```

```html.handlebars
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
htmlbars
```

```htmlbars
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
glimmerandemberjs
```

```glimmerandemberjs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gn
```

```gn
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gni
```

```gni
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gnforninja
```

```gnforninja
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
go
```

```go
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
golang
```

```golang
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
golo
```

```golo
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gololang
```

```gololang
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gradle
```

```gradle
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gf
```

```gf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
grammaticalframework
```

```grammaticalframework
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
graphql
```

```graphql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gql
```

```gql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
groovy
```

```groovy
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gsql
```

```gsql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
haml
```

```haml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
handlebars
```

```handlebars
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
haskell
```

```haskell
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hs
```

```hs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
haxe
```

```haxe
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hx
```

```hx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hlsl
```

```hlsl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
highlevelshaderlanguage
```

```highlevelshaderlanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xml
```

```xml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
html
```

```html
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xhtml
```

```xhtml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rss
```

```rss
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
atom
```

```atom
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xjb
```

```xjb
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xsd
```

```xsd
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xsl
```

```xsl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
plist
```

```plist
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
svg
```

```svg
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
http
```

```http
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
https
```

```https
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hy
```

```hy
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hylang
```

```hylang
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
inform7
```

```inform7
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
i7
```

```i7
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ini
```

```ini
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
toml
```

```toml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
iptables
```

```iptables
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
irpf90
```

```irpf90
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
java
```

```java
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jsp
```

```jsp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
javascript
```

```javascript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
js
```

```js
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jsx
```

```jsx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jolie
```

```jolie
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
iol
```

```iol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ol
```

```ol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
json
```

```json
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jsonc
```

```jsonc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
json5
```

```json5
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jsonata
```

```jsonata
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
julia
```

```julia
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
jl
```

```jl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
kotlin
```

```kotlin
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
kt
```

```kt
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
l4
```

```l4
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
legal
```

```legal
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lang
```

```lang
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lasso
```

```lasso
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ls
```

```ls
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lassoscript
```

```lassoscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tex
```

```tex
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
latex
```

```latex
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ldif
```

```ldif
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
leaf
```

```leaf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lean
```

```lean
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
less
```

```less
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
liquid
```

```liquid
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lisp
```

```lisp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
livecodeserver
```

```livecodeserver
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
livescript
```

```livescript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lookml
```

```lookml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lua
```

```lua
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pluto
```

```pluto
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
luau
```

```luau
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
macaulay2
```

```macaulay2
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
magik
```

```magik
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
makefile
```

```makefile
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mk
```

```mk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mak
```

```mak
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
make
```

```make
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
markdown
```

```markdown
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
md
```

```md
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mkdown
```

```mkdown
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mkd
```

```mkd
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mathematica
```

```mathematica
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mma
```

```mma
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
wl
```

```wl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
matlab
```

```matlab
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
maxima
```

```maxima
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mel
```

```mel
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mayaembeddedlanguage
```

```mayaembeddedlanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mercury
```

```mercury
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
metapost
```

```metapost
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mint
```

```mint
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mips
```

```mips
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mipsasm
```

```mipsasm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mipsassembler
```

```mipsassembler
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mirc
```

```mirc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mrc
```

```mrc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mircscriptinglanguage
```

```mircscriptinglanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mirth
```

```mirth
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mizar
```

```mizar
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mkb
```

```mkb
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mlir
```

```mlir
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mojolicious
```

```mojolicious
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
monkey
```

```monkey
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
moonscript
```

```moonscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
moon
```

```moon
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
motoko
```

```motoko
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mo
```

```mo
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
n1ql
```

```n1ql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
never
```

```never
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nginx
```

```nginx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nginxconf
```

```nginxconf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nim
```

```nim
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nimrod
```

```nimrod
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nix
```

```nix
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
nsis
```

```nsis
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
oak
```

```oak
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ocl
```

```ocl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
objectconstraintlanguage
```

```objectconstraintlanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
objectivec
```

```objectivec
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mm
```

```mm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
objc
```

```objc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
obj-c
```

```obj-c
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
obj-c++
```

```obj-c++
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
objective-c++
```

```objective-c++
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ocaml
```

```ocaml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ml
```

```ml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
odin
```

```odin
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
glsl
```

```glsl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
openglshadinglanguage
```

```openglshadinglanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
openscad
```

```openscad
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
scad
```

```scad
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ruleslanguage
```

```ruleslanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
oracleruleslanguage
```

```oracleruleslanguage
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
oxygene
```

```oxygene
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
papyrus
```

```papyrus
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
psc
```

```psc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
parser3
```

```parser3
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
perl
```

```perl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pl
```

```pl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pm
```

```pm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pf
```

```pf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pf.conf
```

```pf.conf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
phix
```

```phix
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
php
```

```php
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pine
```

```pine
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pinescript
```

```pinescript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
plaintext
```

```plaintext
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
txt
```

```txt
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
text
```

```text
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pony
```

```pony
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pgsql
```

```pgsql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
postgres
```

```postgres
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
postgresql
```

```postgresql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
postgresqlplpgsql
```

```postgresqlplpgsql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
poweron
```

```poweron
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
po
```

```po
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
powershell
```

```powershell
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ps
```

```ps
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ps1
```

```ps1
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
prisma
```

```prisma
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
processing
```

```processing
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
prolog
```

```prolog
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
properties
```

```properties
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
proto
```

```proto
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
protobuf
```

```protobuf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
protocolbuffers
```

```protocolbuffers
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
puppet
```

```puppet
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pp
```

```pp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
python
```

```python
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
py
```

```py
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gyp
```

```gyp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
profile
```

```profile
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pythonprofilerresults
```

```pythonprofilerresults
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
k
```

```k
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
kdb
```

```kdb
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
q
```

```q
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
qsharp
```

```qsharp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
qml
```

```qml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
r
```

```r
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
raku
```

```raku
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
perl6
```

```perl6
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
p6
```

```p6
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pm6
```

```pm6
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rakumod
```

```rakumod
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
pod6
```

```pod6
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rakudoc
```

```rakudoc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rakuquoting
```

```rakuquoting
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rakuregexe
```

```rakuregexe
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cshtml
```

```cshtml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
razor
```

```razor
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
razor-cshtml
```

```razor-cshtml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
razorcshtml
```

```razorcshtml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
reasonml
```

```reasonml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
re
```

```re
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
redbol
```

```redbol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rebol
```

```rebol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
red
```

```red
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
red-system
```

```red-system
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rebolred
```

```rebolred
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rib
```

```rib
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rendermanrib
```

```rendermanrib
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rsl
```

```rsl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rendermanrsl
```

```rendermanrsl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rescript
```

```rescript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
res
```

```res
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
riscv
```

```riscv
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
riscvasm
```

```riscvasm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
riscvassembly
```

```riscvassembly
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
risc
```

```risc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
riscript
```

```riscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
graph
```

```graph
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
instances
```

```instances
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
roboconf
```

```roboconf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
robot
```

```robot
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rf
```

```rf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
robotframework
```

```robotframework
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rpm-specfile
```

```rpm-specfile
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rpm
```

```rpm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
spec
```

```spec
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rpm-spec
```

```rpm-spec
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
specfile
```

```specfile
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rpmspecfiles
```

```rpmspecfiles
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ruby
```

```ruby
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rb
```

```rb
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
gemspec
```

```gemspec
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
podspec
```

```podspec
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
thor
```

```thor
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
irb
```

```irb
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rust
```

```rust
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rs
```

```rs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rvt
```

```rvt
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rvt-script
```

```rvt-script
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
rvtscript
```

```rvtscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sas
```

```sas
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
scala
```

```scala
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
scheme
```

```scheme
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
scilab
```

```scilab
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sci
```

```sci
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
scss
```

```scss
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sfz
```

```sfz
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
shexc
```

```shexc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
shapeexpressions
```

```shapeexpressions
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
shell
```

```shell
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
console
```

```console
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
smali
```

```smali
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
smalltalk
```

```smalltalk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
st
```

```st
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sml
```

```sml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
solidity
```

```solidity
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sol
```

```sol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
spl
```

```spl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
splunkspl
```

```splunkspl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sql
```

```sql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
stan
```

```stan
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
stanfuncs
```

```stanfuncs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
stata
```

```stata
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
iecst
```

```iecst
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
scl
```

```scl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
stl
```

```stl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
structured-text
```

```structured-text
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
structuredtext
```

```structuredtext
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
stylus
```

```stylus
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
styl
```

```styl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
subunit
```

```subunit
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
supercollider
```

```supercollider
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
sc
```

```sc
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
svelte
```

```svelte
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
swift
```

```swift
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tcl
```

```tcl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tk
```

```tk
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
terraform
```

```terraform
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tf
```

```tf
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
hcl
```

```hcl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
terraformhcl
```

```terraformhcl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tap
```

```tap
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
testanythingprotocol
```

```testanythingprotocol
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
thrift
```

```thrift
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
toit
```

```toit
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tp
```

```tp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tsql
```

```tsql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
transactsql
```

```transactsql
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ttcn
```

```ttcn
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ttcnpp
```

```ttcnpp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ttcn3
```

```ttcn3
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
twig
```

```twig
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
craftcms
```

```craftcms
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
typescript
```

```typescript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
ts
```

```ts
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tsx
```

```tsx
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
mts
```

```mts
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
cts
```

```cts
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
unicorn-rails-log
```

```unicorn-rails-log
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
unicornrailslog
```

```unicornrailslog
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
unison
```

```unison
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
u
```

```u
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vala
```

```vala
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vbnet
```

```vbnet
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vb
```

```vb
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vba
```

```vba
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vbscript
```

```vbscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vbs
```

```vbs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
verilog
```

```verilog
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
v
```

```v
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vhdl
```

```vhdl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vim
```

```vim
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vimscript
```

```vimscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
voltscript
```

```voltscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
vss
```

```vss
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lotusscript
```

```lotusscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
lss
```

```lss
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
wgsl
```

```wgsl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xsharp
```

```xsharp
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xs
```

```xs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
prg
```

```prg
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
x
```

```x
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
axapta
```

```axapta
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
x++
```

```x++
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
x86asm
```

```x86asm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
x86assembly
```

```x86assembly
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
x86asmatt
```

```x86asmatt
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
x86assemblyatt
```

```x86assemblyatt
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xl
```

```xl
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
tao
```

```tao
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xquery
```

```xquery
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xpath
```

```xpath
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xq
```

```xq
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
xqm
```

```xqm
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
yml
```

```yml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
yaml
```

```yaml
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
zenscript
```

```zenscript
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
zs
```

```zs
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
zephir
```

```zephir
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```

```text
zep
```

```zep
curl -fsSL --retry 3 -H "Accept: application/json" https://example.com/api
echo "Hello, world!"
```
